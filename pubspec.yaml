name: sambandha_app
description: A clean onboarding flow for SAMBANDHA
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  intl: ^0.20.2  # needed for DateFormat in birthday screen
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/g-logo.png
    - assets/images/logo.png
